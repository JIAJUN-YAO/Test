import React from 'react';

const Settings = () => {
  return (
    <div>
      <header className="Header">
        <h1>SETTING</h1>
      </header>

      <main>
        <div>
          <h1 id="meHeading">My Settings</h1>

          <button type="button" onClick={() => {}}>
            See all profiles
          </button>

          <button type="button" onClick={() => {}}>
            Privacy and Accessibility
          </button>

          <button type="button" onClick={() => {}}>
            Help and Support
          </button>

          <button type="button" onClick={() => {}}>
            Feedback
          </button>

          <button type="button" onClick={() => {}}>
            Log out
          </button>
        </div>
      </main>
    </div>
  );
};

export default Settings;
